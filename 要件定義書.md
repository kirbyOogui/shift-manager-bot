# シフト管理アシスタント 要件定義書

作成日：2026-06-26

---

## 1. プロジェクト概要

LINEにシフト情報を送信するだけで、自動的にスケジュール管理・給与計算・通知を行うシフト管理アシスタント。
自然言語入力に対応するためOpenAI APIを使用し、複数ユーザーが利用可能な設計とする。

---

## 2. システム構成

```
利用者
  |
 LINE
  |
Webhook（HTTPS）
  |
Python（fly.io）
  |
  +-- OpenAI API       （自然言語解析）
  +-- Google Calendar  （スケジュール登録）
  +-- Google Sheets    （データ保存・設定管理）
  |
 LINE（返信・通知）
```

---

## 3. 使用技術

| カテゴリ | 技術・サービス |
|---|---|
| 言語 | Python |
| Webフレームワーク | Flask |
| ホスティング | fly.io（無料枠・1インスタンス固定） |
| スケジューラー | APScheduler |
| メッセージング | LINE Messaging API |
| AI解析 | OpenAI API（GPT-4o-mini） |
| カレンダー | Google Calendar API |
| データ保存 | Google Sheets API |
| 認証 | Googleサービスアカウント |

---

## 4. 機能要件

### 4.1 シフト登録

- 自然言語でシフト情報を送信すると、OpenAIが日付・開始時刻・終了時刻を解析する
- 解析結果をQuick Replyボタン付きの確認メッセージで返信する
- ユーザーが「登録する」を選択した場合のみ登録処理を行う
- 実働時間 = 勤務時間 - 設定済み休憩時間（ユーザーごとに設定）
- Google Calendarにイベントを登録する
- Google Sheetsのシフトデータシートに保存する

**入力例**
```
7/5 9:00~17:00
来週月曜日 午前9時から夕方5時まで
7月5日 9時〜17時
```

**確認メッセージ例**
```
以下の内容で登録しますか？
📅 2026/7/5（日）
⏰ 09:00 〜 17:00
🕐 実働 7時間0分（休憩60分）
[✅ 登録する] [❌ キャンセル]
```

### 4.2 シフト削除

- 削除対象の日付を自然言語で指定する
- 確認メッセージを表示し、ユーザーが承認した場合のみ削除する
- Google CalendarのイベントとGoogle Sheetsの両方から削除する

**入力例**
```
7/5のシフト削除して
7月5日のシフトをキャンセルしたい
```

### 4.3 勤務時間集計

- 今月の勤務データをGoogle Sheetsから取得・集計する
- 勤務日数・合計勤務時間（実働）を返信する

### 4.4 給与予測

- ユーザー設定の時給 × 今月の累計実働時間で計算する
- 月末までの予測は行わず、「現時点での合計」を返す

**返信例**
```
📊 2026年6月の実績
勤務日数：12日
実働時間：84時間30分
現在の給与：¥88,725（時給 1,050円）
```

### 4.5 前日通知

- 毎分APSchedulerでユーザーごとの通知設定時刻をチェックする
- 通知ONのユーザーのみ、翌日に勤務がある場合にLINEで通知を送る
- 当日の重複送信を防ぐため、送信済みフラグをメモリで管理する

**通知メッセージ例**
```
🔔 明日のシフトのお知らせ
📅 2026/7/5（日）
⏰ 09:00 〜 17:00
🕐 実働 7時間0分
```

### 4.6 シフト一覧表示

- 今月のシフト一覧をGoogle Sheetsから取得して返信する

**返信例**
```
📅 2026年6月のシフト一覧
6/2（月）09:00〜17:00（実働7h）
6/5（木）10:00〜18:00（実働7h）
6/9（月）09:00〜17:00（実働7h）
...
```

### 4.7 ユーザー設定管理

- 初回メッセージ時に自動でユーザーをGoogle Sheetsに登録する
- 各ユーザーが自分の設定を変更できる

**設定可能項目**

| 項目 | デフォルト値 | 変更コマンド例 |
|---|---|---|
| 時給 | 1000円 | `時給を1200円に設定して` |
| 休憩時間 | 60分 | `休憩時間を45分に設定して` |
| 通知時刻 | 20:00 | `通知を20時に設定して` |
| 通知ON/OFF | ON | `通知をオフにして` / `通知をオンにして` |

---

## 5. コマンド一覧

| 入力例 | 機能 |
|---|---|
| `7/5 9:00~17:00` | シフト登録 |
| `7/5のシフト削除して` | シフト削除 |
| `シフト一覧` | 今月のシフト一覧表示 |
| `今月の給与` | 勤務時間・給与予測表示 |
| `時給を1200円に設定して` | 時給変更 |
| `休憩時間を45分に設定して` | 休憩時間変更 |
| `通知を20時に設定して` | 通知時刻変更 |
| `通知をオフにして` | 通知無効化 |
| `通知をオンにして` | 通知有効化 |
| `設定確認` | 現在の設定内容を表示 |

---

## 6. データ設計（Google Sheets）

### シート①：シフトデータ

| 列 | 項目 | 型 | 例 |
|---|---|---|---|
| A | LINE UserID | 文字列 | U1234567890 |
| B | 日付 | 日付 | 2026/07/05 |
| C | 開始時刻 | 時刻 | 09:00 |
| D | 終了時刻 | 時刻 | 17:00 |
| E | 実働時間（分） | 数値 | 420 |
| F | Google Calendar EventID | 文字列 | xxxx |
| G | 登録日時 | 日時 | 2026/06/26 10:00 |

### シート②：ユーザー設定

| 列 | 項目 | 型 | 例 |
|---|---|---|---|
| A | LINE UserID | 文字列 | U1234567890 |
| B | 表示名 | 文字列 | 田中 |
| C | 時給（円） | 数値 | 1050 |
| D | 休憩時間（分） | 数値 | 60 |
| E | 通知時刻 | 時刻 | 20:00 |
| F | 通知ON/OFF | 文字列 | ON |
| G | 初回登録日時 | 日時 | 2026/06/26 10:00 |

---

## 7. ファイル構成

```
プロジェクト/
├── main.py              # Flask webhook + APScheduler起動
├── line_service.py      # LINE送受信・Quick Reply
├── shift_parser.py      # OpenAIによる自然言語解析
├── calendar_service.py  # Google Calendar連携
├── sheets_service.py    # Google Sheets連携（シフト・設定）
├── salary_calculator.py # 勤務時間・給与計算（休憩控除含む）
├── notification.py      # 前日通知ロジック
├── user_settings.py     # ユーザー設定管理（時給・休憩時間・通知）
├── config.py            # 環境変数・定数管理
├── requirements.txt     # 依存パッケージ
├── Dockerfile           # コンテナ設定
├── fly.toml             # fly.io設定
└── 記録.txt             # 開発記録
```

---

## 8. 環境変数一覧

| 変数名 | 説明 |
|---|---|
| `LINE_CHANNEL_SECRET` | LINEチャネルシークレット |
| `LINE_CHANNEL_ACCESS_TOKEN` | LINEアクセストークン |
| `OPENAI_API_KEY` | OpenAI APIキー |
| `GOOGLE_CREDENTIALS_JSON` | サービスアカウントJSON（文字列） |
| `GOOGLE_CALENDAR_ID` | 使用するカレンダーID |
| `GOOGLE_SPREADSHEET_ID` | 使用するスプレッドシートID |

---

## 9. 非機能要件

| 項目 | 内容 |
|---|---|
| ホスティング | fly.io 無料枠（shared-cpu-1x / 256MB RAM） |
| インスタンス数 | 1台固定（APSchedulerの重複防止） |
| タイムゾーン | Asia/Tokyo（JST） |
| LINEWebhook署名検証 | 必須（セキュリティ対策） |
| 通知重複防止 | 当日送信済みフラグをメモリで管理 |
| ユーザー識別 | LINE UserIDで識別 |

---

## 10. 開発フェーズ（予定）

| フェーズ | 内容 |
|---|---|
| Phase 1 | 基盤構築（Flask + LINE Webhook + Google Sheets接続確認） |
| Phase 2 | シフト登録・削除機能（OpenAI解析 + Calendar + Sheets） |
| Phase 3 | 集計・給与計算・一覧表示機能 |
| Phase 4 | 前日通知機能（APScheduler） |
| Phase 5 | ユーザー設定管理機能 |
| Phase 6 | fly.ioデプロイ・動作確認 |
